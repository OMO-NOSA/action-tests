name: "My CodeQL Config"

languages: python

queries:
  - uses: security-and-quality  # This preset includes security extended suite

paths-ignore:
  - '**/tests/**'  # Ignore test directories if you don't want them analyzed
  - '**/migrations/**'