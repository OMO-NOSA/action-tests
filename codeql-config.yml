name: "Custom Python Security Analysis"

queries:
  - uses: ./sql_injection.ql
  - uses: ./weak_hashing.ql

paths:
  - src

paths-ignore:
  - '**/test/**'

query-filters:
  - exclude:
      problem.severity:
        - warning
        - recommendation