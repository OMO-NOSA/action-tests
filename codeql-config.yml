name: "Custom Python Security Queries"

queries:
  - uses: ./security-queries.qls  # Assuming you save your QL queries in this file

paths:
  include:
    - src/**/*.py

query-filters:
  - exclude:
      id: 
        - py/unused-local-variable